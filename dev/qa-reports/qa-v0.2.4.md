# k7s v0.2.4 — Recurring QA Index

This file is the index for the recurring QA test cron (`k7s-recurring-qa`, every 10 min).
Each pass writes a per-pass report and appends a row here.

## Coverage rotation

### Already covered (do not re-test unless fixing a regression)
- Helm Market: Charts + Repositories tabs, wizard Version → Values → Review
- Image Registries: registry list, repos, tags drill-down, manifest drill-down
- Templates: deployment form + YAML preview + Apply
- Metrics Explorer: time range buttons, sine wave chart
- Grafana: instance list, preset dashboards, time range
- Endpoints: list + addresses
- Helm InstallWizard Review step button layout — **fixed in 7fe47b6**
- Dashboard header close button — **fixed in 7fe47b6**
- ImageRepoPanel auto-select first registry — **fixed in 0b3a7a8**

### Untested rotation
1. ~~Row context menu — 8 actions~~ — **covered in pass 1**
2. ~~Pod Files panel~~ — **covered in pass 5** (overlay empty-state i18n leak routed through `podFiles.noPod` in `App.tsx`; the panel itself is the entry point, not the empty state — pass-1 already wired the row context menu trigger)
3. ~~Service Topology (d3-force)~~ — **covered in pass 2 + pass 3 + pass 4** (mock bug + dual-ref + Container legend fixed in `3d56a73`; cursor:pointer cleanup in `fd54152`; edges + `--border` alias + force layout in `817c426`)
4. ~~Alerting panel~~ — **covered in pass 8** (empty states + 11 column headers + tab key path typo; collateral fix to the `Open files…` row-action label — both `35944db`)
5. ~~Settings / preferences~~ — **covered in pass 9** (no defects; Esc closes, theme/language switches are immediate, log-buffer clamp holds (9999 → 5000), MCP section + 3 copy buttons render correctly in both locales; pinned `chrome.settings.{title, footerNote, reset}` + `chrome.{copy, copied, copyFailed}` + 8 `settings.*.label` + 3 mcp card titles + section title with 4 new tests — `bb328ef`; observed (not fixed): in-app browser `press_key` ArrowDown routed to the table (not the palette input) once the palette lost focus, and the table treats it as a column-resize — same class as pass-5's "NavList tool row clicks are fiddly" note)
6. ~~⌘K command palette~~ — **covered in pass 1**
7. ~~Detail panel tab cycling `[` / `]`~~ — **covered in pass 7** (no defects; `[` / `]` cycle every kind's tab strip in both directions with wrap, share `tabsFor()` with the tab strip so the strip and keys can't disagree; `isTypingTarget` correctly excludes inputs/textarea/CodeMirror; observed (not fixed): cycling still fires when a row context menu or feature overlay is open — the panel behind them silently advances, but the visual surprise is minimal and the fix is one line if anyone finds it annoying)
8. ~~i18n switch EN ↔ zh~~ — **covered in pass 8** (action menu + Alerting panel: `actions.files` wrong key path, `alerts.tab.*` → `alerts.tabs.*` typo, 11 column headers + 2 empty states hardcoded EN — fixed in `35944db`)
9. ~~Saved Queries CRUD~~ — **covered in pass 11** (Refresh tooltip falsely claimed "Force re-query, ignoring the cache" but `run()` doesn't use a cache — only `runSaved(q, true)` does; tooltip rewritten to "Re-run the current query" / "重新运行当前查询", per-row `↻` tooltip unchanged because that path IS cached; save action button gains `disabled={!saveName.trim()}` matching the Run / Save-toggle style; name input gains Enter submit matching the main query input; 4 new i18n tests pin `refreshTitle` regression (no "cache" / "忽略缓存"), the canonical en/zh values, the 10 `metricsExplorer.saved.*` sub-keys, and `confirmRemove`'s function shape — `ad2b714`; observed (not fixed): "Clear cache" button has no feedback, and the save bar has no "edit existing" affordance — both logged for a future pass; observed (not fixed): in-app Browser render queue was stuck again, same symptom as pass-10 — `/quit` + relaunch recovery path)
10. ~~ImageRepoPanel tags vertical layout polish~~ — **covered in pass 14** (`f8de206`: tag/repo list switched from `display: inline-block` chips to flex-column rows with `margin-left: auto` meta span showing `humanSize(size) · date` when both are non-null — `ImageTag.size` / `.created` are nullable so the meta is conditional; new `.repoName` / `.tagName` / `.tagMeta` classes; collateral i18n key-path fixes: `image.repos.empty` → `image.reposEmpty` and the `image.manifest.*` subtree → flat `image.{mediaType,digest,schemaVersion,size,layers,raw}` — both were dotted paths that couldn't traverse a string leaf and silently fell back to the inline English copy in zh; 4 new i18n tests pin the keys + a regression check that the old dotted paths do NOT resolve; collateral: dropped unused `type Template` import in `templates.test.ts` that surfaced as a tsc warning; observed (not fixed): the `title="Inspect manifest"` tooltip on tag rows is still hardcoded English — pass-1/5/6/8/10/12/13/14's i18n sweep has flagged the bigger leaks but the small hardcoded `title=` attributes throughout the codebase are out of scope for these targeted passes; observed (not fixed): in-app Browser render queue still stuck, fifth consecutive pass — treat as known-stable harness issue; **rotation is now exhausted** — every entry #1 through #14 has been walked and pinned)
11. ~~Sidebar kind/namespace filter~~ — **covered in pass 6** (table empty-state misreport fixed in `b40c91c`; cluster switcher / namespace picker / kind counts all working; "no kind filter for built-in kinds" and "neither kind filter nor namespace filter is persisted" are by-design, logged in the report)
12. ~~Dashboard resource card navigation~~ — **covered in pass 10** (`RESOURCE_KINDS` labels were hardcoded English in zh — same leak class pass-1 / pass-5 / pass-6 / pass-8 fixed for other panels — fixed in `80f70dd`; routes through `kindLabelFor(id, [], locale)` with `?? id` fallback for unknown kinds; 12 new i18n tests pin the canonical en (`Pods / Deployments / …`) and zh (`Pod / Deployment / … / 节点 / 命名空间`) values plus a regression test that zh does NOT render the English pluralised labels; observed (not fixed): in-app Browser render queue was stuck the entire session — the fix is mechanical and the test contract pins the exact zh values, so the lack of in-app visual verification is mitigated; observed (not fixed): Dashboard's `CPU` / `Memory` bar labels at `Dashboard.tsx:115, 130` are still hardcoded English — dictionary already has `dashboard.cpu` / `dashboard.mem` keys, fix is a one-liner per span, but a coordinated pass should also touch the detail panel's metrics tab)
13. ~~Templates Ingress / ConfigMap variants~~ — **covered in pass 13 + pass 22** (pass-13 fixed 3 form defects: number inputs without `min`/`max`, no Enter-to-submit, dropped `ApplyResult.namespace` in the result list — `ed3b318`; pass-22 closed the remaining follow-up by adding the `required` HTML5 attribute to text/number inputs — `d9c72e4`; `TemplateParam` now carries an optional `required?: boolean`, the form mirrors it as the native attribute, and 2 new tests pin the policy; the renderer's `||` fallback remains a safety net for programmatic callers. observed (not fixed, leftover from pass-13): template `title` / `description` still hardcoded English in the registry, but **pass-16 already fixed this** via `tpl.titles.<id>` / `tpl.descs.<id>`; observed (not fixed): in-app Browser render queue is still stuck, thirteenth consecutive pass — treat as known-stable harness issue)
14. ~~Helm Market Repositories CRUD~~ — **covered in pass 12** (the add-repository form was a bare `<div>` with no `<form onSubmit>`, no `required`, no button `type` attributes — Enter didn't submit and empty fields went silently to the provider; fixed in `30bc5e5`; `form onSubmit` + `required` on name/url + `type="url"` on the url input + `type="submit"` / `type="button"` on the buttons; 3 new i18n tests pin `helm.repos.*` + `helm.repos.form.*` + `helm.empty.{noMatch, noRepos}` in both locales plus `confirmRemove`'s function shape; observed (not fixed): Add button has no loading / disabled state during submit (race allows double-click — Rust backend de-dupes by name so worst case is a confusing error); observed (not fixed): name input could gain a `pattern` attribute to surface the `/`/` `/`\` charset error client-side; observed (not fixed): in-app Browser render queue is still stuck — third consecutive pass with the same symptom, treat as known-stable and rely on code review + HMR `curl` + test pinning for verification)

## Pass log

| # | Date (Asia/Shanghai) | Area tested | Findings | Fixes | Report |
|---|---|---|---|---|---|
| 0 | 2026-08-03 (bootstrap) | — | bootstrap index | — | this file |
| 1 | 2026-08-03 (first real pass) | ⌘K command palette (EN + zh) | 1 high: action labels + hints were hardcoded English even in zh locale. 1 low (not fixed): object-candidate kind-hint stays English in zh. | commit `29b0fd5` — palette action labels and hints routed through `chrome.palette.actions.*` in both locales, with paletteStr helper that falls back zh → en → hardcoded; 4 new tests; tsc / vitest 309 / cargo check all clean. | [qa-v0.2.4-pass-2026-08-03.md](qa-v0.2.4-pass-2026-08-03.md) |
| 1 | 2026-08-03 | Row context menu (8 actions) + global Esc cascade | Overlay (Pod Files, etc.) ignored Esc — feature gaps in `useGlobalKeys`; missing `actions.files` in zh dict (low) | Esc → `closeOverlay()` in cascade; new test in `useGlobalKeys.test.ts` — **4b6496f** | [pass-1](qa-v0.2.4-pass-2026-08-03.md) |
| 2 | 2026-08-03 | Service Topology overlay (d3-force) | 1 high: `listEndpointAddresses` mock ignored `ns`/`name` and always returned nginx pods, so redis was wired to nginx-1/nginx-2. 1 medium: Container legend chip with no Container kind nodes. 1 low: `svgRef` attached to both `.canvas` div and inner `<svg>`. 1 low: ResizeObserver only fired on change so first paint used 800×500 default. **Known issue (not fixed)**: 0 `<line>` elements render despite positions Map being populated (7 keys, 5 links). | `3d56a73` — Mock branches per slice (redis→redis-0, nginx→nginx-1/2); Container legend chip dropped; svgRef cleaned up (HTMLDivElement on the div only); updateSize() runs on mount. tsc / vitest 309 / cargo check all clean. | *index-only summary* (per-pass file written by the previous cron session but not committed; the row above captures the findings) |
| 3 | 2026-08-03 | Service Topology overlay (re-test after `3d56a73`) | 1 medium: `cursor: pointer` on Service list items advertised a click the panel doesn't perform. 2 low (not fixed): dead i18n keys `topology.pick`/`topology.loading` and dead CSS rules (`.graph`/`.column`/`.arrow`/`.nodePrimary`/`.nodeOk`/`.nodeBad`) — left as cleanup for a future pass. **Known issue (not fixed)**: graph edges are nearly invisible (`stroke=var(--border)` `strokeWidth=0.7` `opacity=0.5`) — see pass-2's row and the new pass-3 "Notes for next pass". | commit `fd54152` — dropped `cursor: pointer` and dead `.itemActive` rule on `.item` in `TopologyPanel.module.css`; tsc / vitest 309 / cargo check all clean; pushed to `origin/main`. | *index-only summary* (no per-pass file — work was small and the diff is the report) |
| 4 | 2026-08-03 | Service Topology overlay (3rd pass) + cross-cutting CSS tokens | 1 high: `var(--border)` undefined in 3 theme blocks — every `border: 1px solid var(--border)` in ~30 components rendered without its separator. 1 high: topology graph edges were invisible because `positions.get(l.source as string)` returned undefined for d3-resolved node references. 1 medium: topology cluster jammed against the top of the viewBox because `forceCenter` only translates the *mean*; added `forceX`/`forceY` strength 0.08 to land each cluster in the middle. 1 low (test-only): `theme.test.ts` over-strict — `var(--border-default)` shares the same string in both palettes by design. | commit `817c426` — added `--border: var(--border-default)` to `:root`, `[data-theme="light"]`, and `[data-theme="light"] [data-surface="panel"]`; re-typed `GraphLink.source`/`.target` as `Link["source"]` and narrowed at the call site; added `forceX`/`forceY` alongside `forceCenter`; theme test now skips `var(...)` references. tsc / vitest 309 / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-4.md](qa-v0.2.4-pass-2026-08-03-4.md) |
| 5 | 2026-08-03 | Pod Files panel (rotation #2) | 1 low: `App.tsx:85` overlay empty state hardcoded English even in zh locale; both EN and ZH dicts already had `podFiles.noPod` (the strings were sitting unused, same leak class pass-1 fixed for `⌘K`). Observed (not fixed): no test coverage for `PodFilesPanel` — `dirty` flip and the tar-download blob path are easy targets for a future `*.test.tsx`. | `50f20ad` — `App.tsx:14, 52, 86` swap the hardcoded string for `t("podFiles.noPod", "Open Pod Files from a Pod's row context menu.")`. tsc / vitest 309 / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-5.md](qa-v0.2.4-pass-2026-08-03-5.md) |
| 6 | 2026-08-03 | Sidebar / topbar / table filter, namespace picker, kind counts (rotation #11) | 1 low: `ResourceTable.tsx:339` empty state always said "no resources match filter" — wrong when the *namespace picker* was the cause (e.g. `ns: default` on a Pods view that has no default pods) and equally wrong on a kind with zero rows (e.g. HPAs on a cluster that doesn't run any). Observed (not fixed, by design): no kind filter for the built-in kinds; neither kind filter nor namespace picker persists (the brief's claim about localStorage was wrong — `defaultNamespace` is a connect-time default, not a per-session persist); Custom-section filter gated at > 8 kinds. | `b40c91c` — added `table.emptyNone` to both locales, branched the empty state on `tableFilter.trim() === ""`; 2 new i18n tests. tsc / vitest 311 (309 + 2 new) / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-6.md](qa-v0.2.4-pass-2026-08-03-6.md) |
| 7 | 2026-08-03 | Detail panel `[` / `]` tab cycling (rotation #7) | **No defects.** The handler shares `tabsFor()` with the tab strip, so the strip and keys can't disagree. Pods (6 tabs), ReplicaSets (2 tabs), and the `selectionPatch` default (`logs` for pods, `yaml` for non-pods) all cycle correctly in both directions with wrap. `isTypingTarget` excludes `<input>`/`<textarea>`/`[contenteditable]` (covers the CodeMirror YAML editor), so cycle keys don't hijack filter input or the YAML editor. Observed (not fixed): cycle still fires when a row context menu or a feature overlay is open — the panel behind them silently advances. | none (no defects to fix). tsc / vitest 311 / cargo check all clean; working tree clean. | [qa-v0.2.4-pass-2026-08-03-7.md](qa-v0.2.4-pass-2026-08-03-7.md) |
| 8 | 2026-08-03 | Alerting overlay (rotation #4) | 2 high: `AlertsPanel.tsx` empty states + 11 column headers hardcoded English (`Alert / Severity / State / Summary / Active since` for alerts, `Matchers / Comment / Created by / Starts / Ends / Status` for silences); tab key path typo (`alerts.tab.*` vs `alerts.tabs.*`) so `zh` UI still read "Alerts (1)" / "Silences (0)". 1 medium (collateral, piggybacked): `ActionList.tsx:315` used `tr("actions.files", "Open files…")` instead of `tr("actions.labels.files")` — the dict already had the right key (`actions.labels.files`), the call site pointed at a non-existent path, so the English fallback rendered in every locale. Same class of leak as the Alerting tab typo. | commit `35944db` — added `alerts.cols.*` (11 keys) to both EN/ZH dicts; routed the two empty states and 11 column headers through `t()` with English fallbacks; fixed the tab key typo (`alerts.tab.*` → `alerts.tabs.*`); collateral fix to the `Open files…` row-action label; 3 new i18n tests pin the new keys in both locales. tsc / vitest 314 (311 + 3 new) / cargo check all clean; pushed to `origin/main`. **Browser-verified in zh**: Alerting panel title `告警`, tabs `告警 (1)` / `静默 (0)`, column headers `告警 / 严重程度 / 状态 / 摘要 / 激活时间`, silences empty state `无静默`. Pod row context menu shows `打开文件…` (was `Open files…`). | [qa-v0.2.4-pass-2026-08-03-8.md](qa-v0.2.4-pass-2026-08-03-8.md) |
| 9 | 2026-08-03 | Settings / preferences panel (rotation #5) | **No defects.** Esc closes, theme/language switches are immediate, log-buffer clamp holds (9999 → 5000), MCP section + 3 copy cards render in both locales with the right `codePath` + `复制` button labels. All eight row labels and hint strings route through `useTranslation()` in both EN/ZH. Observed (not fixed): in-app browser `press_key` ArrowDown can route to the table (not the focused input) once the palette closes, and the table treats it as a column-resize — same class of test-harness oddity as pass-5's "NavList tool row clicks are fiddly" note. | `bb328ef` — added 4 i18n regression tests that pin `chrome.settings.{title, footerNote, reset}` + `chrome.{copy, copied, copyFailed}` + 8 `settings.*.label` + 3 mcp card titles + section title, so a future refactor can't drop them the way `chrome.palette.actions.*` / `topology.*` were dropped. tsc / vitest 318 (314 + 4 new) / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-9.md](qa-v0.2.4-pass-2026-08-03-9.md) |
| 10 | 2026-08-03 | Dashboard resource card navigation (rotation #12) | 1 high: `RESOURCE_KINDS` at `Dashboard.tsx:25-48` hardcoded 9 English card labels (`Pods / Deployments / …`) — zh UI rendered the same English labels, same leak class as the pass-1 / pass-5 / pass-6 / pass-8 fixes for other panels. Observed (not fixed): in-app Browser render queue was stuck the entire session (every `inspect` / `navigate` / `wait` returned `Background Browser render queue wait timed out`); fix is mechanical and the test contract pins the exact zh values, so the lack of in-app visual verification is mitigated. Observed (not fixed): Dashboard's `CPU` / `Memory` bar labels at `Dashboard.tsx:115, 130` are still hardcoded English — dictionary already has `dashboard.cpu` / `dashboard.mem` keys, fix is a one-liner per span, but a coordinated pass should also touch the detail panel's metrics tab. | `80f70dd` — `RESOURCE_KINDS` no longer carries `label` (only `id` + `color`); `useTranslation()` now also returns `locale`; resource grid maps each id through `kindLabelFor(id, [], locale) ?? k.id`; doc-block on the constant records the rationale. 12 new i18n tests pin every dashboard kind × locale, the canonical en labels, the canonical zh labels (`Pod / Deployment / … / 节点 / 命名空间`), and a regression test that zh does NOT render the English pluralised labels. tsc / vitest 330 (318 + 12 new) / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-10.md](qa-v0.2.4-pass-2026-08-03-10.md) |
| 11 | 2026-08-03 | Saved Queries CRUD (rotation #9) | 1 medium: outer Refresh button's tooltip `"Force re-query, ignoring the cache"` is a lie — `run()` calls `metricsQuery` / `metricsQueryRange` which don't use a cache, so the tooltip promises a bypass that does not exist (the cache lives only in `saved_queries::run_saved` and the per-row `↻` is the only thing that bypasses it). 2 low: save action button is always enabled; click with empty name was a silent no-op. Name input has no Enter-key submit, unlike the main PromQL input. Observed (not fixed): "Clear cache" button has no visual feedback. Observed (not fixed): save bar has no "edit existing" affordance — typing the same name silently overwrites. Observed (not fixed): in-app Browser render queue was stuck again, same as pass-10. | `ad2b714` — `metricsExplorer.refreshTitle` en `"Force re-query, ignoring the cache"` → `"Re-run the current query"`, zh `"强制重新查询,忽略缓存"` → `"重新运行当前查询"`, JSX `title` fallback updated to match; save action button gains `disabled={!saveName.trim()}` matching the Run / Save-toggle style; name input gains `onKeyDown` Enter submit matching the main query input. 4 new i18n tests pin the regression (no "cache" / "忽略缓存" in `refreshTitle`), the canonical en / zh values, the 10 `metricsExplorer.saved.*` sub-keys, and `confirmRemove`'s function shape. tsc / vitest 334 (330 + 4 new) / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-11.md](qa-v0.2.4-pass-2026-08-03-11.md) |
| 12 | 2026-08-03 | Helm Market Repositories CRUD (rotation #14) | 1 high: the add-repository form was a bare `<div>` with three `<input>`s and two `<button>`s — no `<form onSubmit>`, no `required`, no button `type` attributes. Enter on any input was a no-op; clicking `Add` with empty name or url sent the empty string to the Rust backend and surfaced a generic error in the top-of-panel banner (user had to scroll up to see it); cancel could accidentally submit. Every other overlay in the app (ImageRepoPanel registry form, MetricsExplorer query bar, the saved-queries save bar) already supports Enter-to-submit. Observed (not fixed): Add button has no loading/disabled state during submit. Observed (not fixed): name input could gain a `pattern` attribute to surface the `/`/` `/`\` charset error client-side. Observed (not fixed): in-app Browser render queue was stuck — third consecutive pass with the same symptom. | `30bc5e5` — `<div>` → `<form onSubmit>` with `e.preventDefault()`; name and url inputs gain `required` (url input also gains `type="url"`); Add button `type="submit"`, cancel `type="button"`. 3 new i18n tests pin `helm.repos.*` (8 keys) + `helm.repos.form.*` (5 keys) + `helm.empty.{noMatch, noRepos}` in both locales, plus `confirmRemove`'s function shape. tsc / vitest 337 (334 + 3 new) / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-12.md](qa-v0.2.4-pass-2026-08-03-12.md) |
| 13 | 2026-08-03 | Templates Ingress / ConfigMap variants (rotation #13) | 2 high + 1 medium. **High #1**: number inputs had no `min`/`max` so `replicas=0` / `port=99999` echoed into the input but the YAML preview showed the clamped value (`replicas: 1` / `port: 65535`) — the form's value and the rendered preview disagreed with no UI feedback (defensive `clampInt` in the renderer was meant as a server-side safety net but without native HTML5 validation the user had no way to know the typed value was rejected). **High #2**: no Enter-to-submit (same class as pass-12 Helm Market fix; the form was a `<div>` of inputs + `<button onClick={apply}>` with no `<form onSubmit>` wrapper, no `type="submit"`, no `onKeyDown` Enter handler). **Medium #3**: apply result list rendered `{r.action} {r.kind}/{r.name}` and dropped `r.namespace` — the `ApplyResult` type already exposes `namespace: string` and every backend returns it, so non-default-namespace creates looked identical to default-namespace creates. Observed (not fixed): empty text fields silently fall through to defaults (the renderer's `v.name || "my-app"` fallback means a cleared input + Apply creates the default-named resource); `placeholder={p.default}` partially addresses this and `min`/`max` covers the numeric side, but a coordinated `required` policy needs a separate decision. Observed (not fixed): template `title` / `description` still hardcoded English in the registry (`"Ingress (Nginx)"` / `"Single-container Deployment with a Service (ClusterIP)."` render in zh locale unchanged), same i18n leak class as pass-1/5/6/8/10 — refactor is `tpl.titles.<id>` / `tpl.descs.<id>` keys across all three templates at once. Observed (not fixed): in-app Browser render queue stuck, fourth consecutive pass with the same symptom — treat as known-stable harness issue. | `ed3b318` — `TemplateParam` gains `min` / `max` / `placeholder`; `deployment.replicas` 1..100, `deployment.port` + `ingress.port` 1..65535; form is now a `<form onSubmit>` with `type="submit"` on Apply and a `formRoot` CSS class matching the previous fragment's column-with-gap shape; result list renders `${r.namespace}` when present; `useState<ApplyResult[]>` is now the canonical type from `providers/types` so `namespace` is visible at the call site. 17 new tests in `src/lib/templates.test.ts` (337 → 354) pin registry ids, `defaultValuesFor` round-trip, every number param's bounds, `clampInt` behaviour for out-of-range and NaN values, and Ingress / ConfigMap YAML shape. tsc / vitest 354 (337 + 17 new) / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-13.md](qa-v0.2.4-pass-2026-08-03-13.md) |
| 14 | 2026-08-03 | ImageRepoPanel tags vertical layout polish (rotation #10) | 1 primary + 2 high + 1 low. **Primary**: tag/repo list was `display: inline-block` chips with no per-row metadata — fine for 3-5 tags, but the list only ever rendered `tt.name` (no size, no date) so the user had no way to compare tags at a glance. **High #1** (collateral i18n leak, same class as pass-1/5/6/8/10): `t("image.repos.empty", ...)` was used at the empty-repos state, but the dictionary only ships `image.reposEmpty` — a string leaf is not walkable, so the dotted path resolved to undefined in both locales and the inline English copy always rendered (zh ships `无镜像(或仓库不支持 /v2/_catalog)` but it never reached the UI). **High #2** (collateral i18n leak, same class): the whole `image.manifest.*` subtree (`mediaType` / `digest` / `schemaVersion` / `size` / `layers` / `raw`) was used in the TSX but the dictionary defines them flat at `image.mediaType` / `image.digest` / etc. — same dotted-path-traversal bug, so the manifest-table headers and the layers sub-table headers all read English in zh even though the dict has the translations. **Low** (collateral test): pre-existing unused `type Template` import in `templates.test.ts` (leftover from `ed3b318`); tsc was warning about it. Observed (not fixed): `title="Inspect manifest"` on the tag row is still hardcoded English — pass-1/5/6/8/10/12/13/14's i18n sweep has flagged the bigger leaks but the small hardcoded `title=` attributes are out of scope. Observed (not fixed): in-app Browser render queue stuck, fifth consecutive pass — treat as known-stable harness issue. **Rotation is now exhausted** — every entry #1 through #14 has been walked and pinned. | `f8de206` — `.repos` / `.tags` switch to `flex-direction: column` with 2px gap; `.repo` / `.tag` switch to `display: flex` with `align-items: center` and `gap: var(--space-2)`; new `.repoName` / `.tagName` (mono, `flex: 0 0 auto`) + `.tagMeta` (muted, `margin-left: auto`); tag meta `${humanSize(size)} · ${created.slice(0, 10)}` is gated on both fields being non-null per `ImageTag`'s nullable `size` / `created`; 7 `t("image.manifest.*", ...)` call sites → flat `image.{...}`; 1 `t("image.repos.empty", ...)` → `image.reposEmpty`; dropped unused `type Template` import. 4 new i18n tests in `image registries panel keys` describe pin: every key the panel touches (chrome + form sub-keys), canonical en/zh values for `reposEmpty` and the 6 manifest-table keys, plus regression checks that the dotted `image.repos.empty` and `image.manifest.*` paths do NOT resolve to the same value (so a future refactor that reintroduces the nested keys trips the test). tsc / vitest 358 (354 + 4 new) / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-14.md](qa-v0.2.4-pass-2026-08-03-14.md) |
| 15 | 2026-08-03 | Detail-panel tab + Dashboard residual i18n sweep (post-rotation follow-up) | 2 high + 2 medium + 1 low + 1 high. **High #1** (Dashboard): `<span>CPU</span>` / `<span>Memory</span>` at `Dashboard.tsx:128, 143` were hardcoded English — the dict already shipped `dashboard.cpu` / `dashboard.mem` in both locales, but no call site read them. Pass-10 specifically flagged this as a follow-up: "a coordinated pass should also touch the detail panel's metrics tab". **High #2** (NodeShellTab): six hardcoded English strings at lines 71, 99, 113, 123, 131, 146 — gate button, header labels, end-session button, start-again button, and two `||` fallbacks (`"session ended"` / `"session closed"`). The user-facing copy in the privileged debug session flow. **Medium #3** (EventsTab): `EventsTab.tsx:45` had the literal `"no recent events — events expire after ~1h"` — dict only had `events.loading` / `events.hint`. **Medium #4** (PropertiesTab): `title={\`Go to ${target.kind} ${target.name}\`}` on a cross-reference link at line 147 — a function-form key (`properties.navTitle(kind, name)`) was the right shape but didn't exist. **Low #5** (ShellTab): the `↻ reconnect` label and `reason || "session ended"` fallback at lines 67, 127. **Low #6** (LogsTab): `{filtered.length} lines` footer at line 194. **Medium #7** (PodMetricsTab): three-line hardcoded English body under the "waiting for first sample" state at lines 44-46 — the diagnostic that helps the user understand the metrics-server failure mode. Observed (not fixed): in-app Browser render queue stuck, sixth consecutive pass. Observed (not fixed, follow-ups from prior passes): small hardcoded `title=` attributes (e.g. `title="Inspect manifest"`), pattern attribute on Helm Market name input, Templates empty text fields silently falling through to defaults. **The v0.2.4 rotation plus its post-rotation residual sweep is now complete** — the next pass should either pick a deeper sub-area (e.g. MCP server health UI from pass-14's suggestion) or shut the cron down per the self-cleanup heuristic (the rate of finding new issues is slowing: pass-12/13/14/15 all found substantive issues but each pass's bug class is shrinking). | `5347412` — Dashboard.tsx 2 lines (CPU / Memory); EventsTab.tsx 1 line (`events.empty`); PropertiesTab.tsx 3 lines (`NavLink` gains `useTranslation`, `properties.navTitle(kind, name)`); ShellTab.tsx 2 lines (`shell.reconnect` + `shell.endedFallback`); NodeShellTab.tsx 6 callsites (7 new `nodeShell.*` keys: `startBtn` / `starting` / `nodeLabel` / `endSession` / `startAgain` / `endedFallback` / `closedFallback`); LogsTab.tsx 1 line (`logs.linesCount(n)`); PodMetricsTab.tsx 4-line body → 1 line (`podMetrics.waitingBody`); dictionaries.ts type + EN + ZH gains 13 new keys; i18n.test.ts new `describe("detail-panel tab + dashboard i18n (pass-15 sweep)", ...)` block with 7 tests pinning the canonical en / zh values for every new key. tsc / vitest 365 (358 → 365, +7 new) / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-15.md](qa-v0.2.4-pass-2026-08-03-15.md) |
| 16 | 2026-08-03 | Template picker title/description i18n + ImageRepoPanel tag-row tooltip (post-rotation follow-up, round 2) | 1 high + 1 low. **High #1** (Templates): `TemplatePicker.tsx:86, 87, 94` rendered `tt.title` / `tt.description` / `selected.title` directly from the registry — zh user saw English `Deployment / Ingress (Nginx) / ConfigMap` and the English descriptions in the list + form heading. Pass-13 flagged this as a follow-up: "refactor is `tpl.titles.<id>` / `tpl.descs.<id>` keys across all three templates at once". **Low #2** (ImageRepoPanel): `title="Inspect manifest"` HTML attribute on each tag row at `ImageRepoPanel.tsx:297` — pass-14 specifically flagged this residual `title=` as a follow-up. Observed (not fixed): in-app Browser render queue stuck, seventh consecutive pass — treat as known-stable harness issue. **Open follow-ups**: pass-12 (Helm Market `pattern` attribute + Add button loading state), pass-11 (saved-queries "Clear cache" feedback + "edit existing" affordance), pass-13 (Templates empty text fields `required` policy), pass-14 (other small hardcoded `title=` attributes throughout the codebase). | `6e9a63b` — `tpl.titles.{deployment,ingress,configmap}` and `tpl.descs.{deployment,ingress,configmap}` added to both EN/ZH dicts; `TemplatePicker` routes all 3 sites through `t("tpl.titles." + id, tt.title)` / `t("tpl.descs." + id, tt.description)` with the registry string as the English fallback. Collateral: `image.inspectTitle` added to both dicts; `ImageRepoPanel.tsx:297` `title=` attribute routes through `t("image.inspectTitle", "Inspect manifest")`; zh "查看清单" uses the same `清单` noun as the panel's `image.manifest` chrome. 6 new i18n tests (5 for `tpl.titles/descs` per-id + registry-canonical equality + zh deployment desc contents + fallback path; 1 for `image.inspectTitle` with zh noun-consistency check). tsc / vitest 371 (365 → 371, +6 new) / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-16.md](qa-v0.2.4-pass-2026-08-03-16.md) |
| 17 | 2026-08-03 | Chrome i18n sweep round 3 — StatusBar / ClusterSwitcher / MetricsExplorer InstantTable (post-rotation follow-up, round 3) | 3 high + 2 low (observed). **High #1** (StatusBar): 6 labels (`api` / `nodes` / `ready` / `cpu` / `mem` / `kubectl ctx:`) at `StatusBar.tsx:40, 44, 48, 52, 56` rendered as raw English even in zh — the always-visible bottom strip every session sees. The dict already shipped `chrome.statusbar.*` keys, but they were function-shaped full sentences (`api: (ms) => "api: ${ms}ms"`) that no call site ever read, so the StatusBar fell through to raw literals. **High #2** (ClusterSwitcher): 4 strings (`connected · v1.28.0` / `connecting…` / `disconnected` / `no cluster`) at `ClusterSwitcher.tsx:69, 167, 170, 172` rendered as raw English — the status line right under the cluster name at the top of the sidebar. **High #3** (MetricsExplorer InstantTable): 2 column headers (`Series` / `Value`) at `MetricsExplorer.tsx:353-354` rendered as raw English. **Low #4** (observed, not fixed): `title="Grafana"` on the iframe at `GrafanaPanel.tsx:272` — brand name, identical in every locale, lowest-leverage find. **Low #5** (observed, not fixed): `kubernetes monitor` brand subtitle at `Sidebar.tsx:21` — brand voice, would lose character in zh. Observed: in-app Browser render queue stuck, eighth consecutive pass — treat as known-stable harness issue. | `63ee32c` — `chrome.statusbar` block reshaped from function-shaped full sentences to label-only string leafs (`api` / `nodes` / `ready` / `cpu` / `mem` / `kubectlCtx`) — the old function keys were dead code, no call site ever read them. New `chrome.clusterSwitcher` block (1 function `connected(version) → "connected · v1.28.0"` + 3 strings `connecting` / `disconnected` / `noCluster`). New `metricsExplorer.instantTable` block (2 strings `series` / `value`). StatusBar.tsx now imports `useTranslation`; the 6 labels route through `t()` while preserving the label / value split (`<b>` on the value). ClusterSwitcher's `statusDisplay(phase, version, t)` helper accepts the bound `t` and returns the 3 status strings via `t("chrome.clusterSwitcher.connected", version)` / `t("chrome.clusterSwitcher.connecting")` / `t("chrome.clusterSwitcher.disconnected")`; the `?? "no cluster"` fallback at line 69 → `?? t("chrome.clusterSwitcher.noCluster")`. MetricsExplorer's `InstantTable` gains `const { t } = useTranslation();` and the 2 column headers route through `t()`. 5 new i18n tests pin the en / zh values for every new key, the zh `节点` / `就绪` / `已连接` / `连接中…` / `已断开` / `未选择集群` / `序列` / `值` nouns, the `undefined` version fallback in `connected()`, and regression assertions that zh does not render the English labels and the old function-shaped `statusbar.api` no longer contains `ms` / `—` (so a future refactor that re-introduces the function shape trips the test). tsc / vitest 376 (371 → 376, +5 new) / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-17.md](qa-v0.2.4-pass-2026-08-03-17.md) |
| 18 | 2026-08-03 | ActionList scale + port-forward form polish + HelmMarket add-repo follow-ups (post-rotation follow-up, round 4) | 3 high + 2 low. **High #1** (ActionList scale form): the panel was a `<div>` (not `<form>`), so Enter didn't submit; the value was a `<span>` with only `+` / `−` 1 buttons (no typing path — a 3→30 StatefulSet needs 27 clicks); Apply was a `<div>` with `aria-disabled={busy}` only (a11y hint, not a click guard — double-click queues two scale calls). **High #2** (ActionList port-forward form): same `<div>`-not-`<form>` + click-only Apply + `aria-disabled`-only defects; the port input's `setPort(Number(e.target.value))` accepted `NaN` (clearing the input sends `NaN` to the backend) and out-of-range values. **High #3** (HelmMarket add-repo): pass-12's two explicit follow-ups both closed — name input gains `pattern="[a-z0-9][a-z0-9-]*"` + a `title=` tooltip describing the rule, and the form gains a `submitting` state that `disabled`s all 3 inputs and both buttons during the in-flight request. **Low #4** (ActionList confirm dialog): the literal `"…"` in-flight indicator on Delete / Restart / Drain routes through `t("actions.confirming", "…")` (en/zh both `"…"`, value-add is structural). **Low #5** (ActionList confirm dialog): the click handlers gain an `if (busy) return;` early-return so a fast double-click on Delete can't queue two destructive actions. Observed (not fixed): in-app Browser render queue stuck, ninth consecutive pass — treat as known-stable harness issue. Observed (not fixed, follow-ups still on the queue): pass-11 (saved-queries "Clear cache" feedback + "edit existing" affordance), pass-13 (Templates empty text fields `required` policy), pass-15 (MCP server health / latency), pass-16 (resource table column resize / reorder UX), pass-17 (cluster switcher mid-dot version truncation). **The v0.2.4 rotation + 4 rounds of post-rotation polish is now substantive** — every major form surface (HelmMarket add-repo, Templates, ActionList scale, ActionList port-forward) has been audited against the pass-12/13 fix pattern (`<form onSubmit>` + Enter-to-submit + real `disabled` + in-flight text + clamp + `pattern` where relevant). The rate of finding new issues is steady but the defect class is narrowing. | `c901b8b` — `ActionList.tsx` scale form: `<form onSubmit>` + Enter-to-submit; the `<span>` is replaced with a `<input type="number" min={0}>` (typed value is `Number.parseInt`'d, `NaN` falls back to `0`, `Math.max(0, n)` keeps it non-negative); the `−` button is gated on `replicas > 0`; Apply becomes a real `<button type="submit" disabled={busy}>` with "Applying…" / "正在调整…" in-flight text; a `replicas` / `副本数` label sits next to the input. Port-forward form: same `<form onSubmit>` + real disabled Apply + "Forwarding…" / "正在转发…" in-flight text; port input clamps to `[1, 65535]` on every keystroke; a `port` / `端口` label next to the input. Confirm dialog: the literal `"…"` routes through `t("actions.confirming", "…")`; click handlers gain `if (busy) return;` early-return. `HelmMarket.tsx` add-repo form: `submitting` state, all 3 inputs and both buttons `disabled={submitting}`, Add button shows "Adding…" / "正在添加…", name input gains `pattern="[a-z0-9][a-z0-9-]*"` + a `title=` tooltip describing the rule. `ActionList.module.css`: `.cancelBtn` / `.applyBtn` / `.dangerBtn` gain the small resets (`background: transparent; font: inherit; line-height: 1.4;`) that make a real `<button>` render the same as the `<div>` the action menu used to use; the in-flight visual rule gains a `[disabled]` selector next to `[aria-disabled="true"]`. 7 new i18n leaves (`actions.scaleForm.{applying, replicasLabel}` / `actions.forwardForm.{applying, portLabel}` / `actions.confirming` / `helm.repos.form.{adding, nameTitle}`); the existing `helm.repos.form.*` iteration adds `"adding"` and `"nameTitle"`; new `describe("action list scale + forward form keys (pass-18)", ...)` block with 4 tests (en/zh for the 5 string leaves, the confirming ellipsis, and a regression check that `nameTitle` and `name` are distinct). tsc / vitest 380 (376 → 380, +4 new) / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-18.md](qa-v0.2.4-pass-2026-08-03-18.md) |
| 19 | 2026-08-03 | MetricsExplorer saved-queries CRUD polish — overwrite hint + save loading + cache feedback (post-rotation follow-up, round 5) | 3 high + 1 low. **High #1** (save bar): the `Save` action button was always labelled the same way regardless of whether the typed name matched an existing saved query — a user overwriting `Node CPU` (or any name they'd forgotten they had) got no visual hint until the `savedQueriesUpsert` silently mutated state. Pass-11 explicitly flagged this as a follow-up. **High #2** (save action): the button had `disabled={!saveName.trim()}` only — no in-flight text, no early-return on the in-flight state — so a double-click (or Enter-spam) could queue two upserts. Same defect class as pass-12 (HelmMarket add-repo), pass-13 (Templates Apply), pass-18 (ActionList scale / forward). **High #3** (Clear cache): the pre-refactor onClick just fired `savedQueriesClearCache()` with no state change — user clicked, nothing visible happened, no idea whether the click landed. Pass-11 explicitly flagged this as a follow-up. **Low #4** (save bar inputs): the name / note inputs were never `disabled` during the in-flight save, so the user could keep typing and press Enter again (the second request was already dropped by the `saving` early-return, but the visual feedback was missing). Observed (not fixed): in-app Browser render queue stuck, 10th consecutive pass — treat as known-stable harness issue. Observed (not fixed, follow-ups still on the queue): pass-13 (Templates empty text fields `required` policy), pass-15 (MCP server health / latency), pass-16 (resource table column resize / reorder UX), pass-17 (cluster switcher mid-dot version truncation + WatchFooter pulsing dot). **The v0.2.4 rotation + 5 rounds of post-rotation polish is now substantive** — every major form surface (HelmMarket add-repo, Templates, ActionList scale, ActionList port-forward, MetricsExplorer save bar, MetricsExplorer cache-bust) has been audited against the pass-12/13/18/19 fix pattern (`<form onSubmit>` + Enter-to-submit + real `disabled` + in-flight text + clamp + `pattern` where relevant + an explicit overwrite affordance where the action can mutate user data). | `51caba2` — `MetricsExplorer.tsx`: `saving` state guards the action button + greys the 2 inputs during the in-flight `savedQueriesUpsert`; button text is a 3-way `saving` / `isOverwrite ? updateAction : saveAction`; new `cacheState: "idle" | "ok"` mirrors the McpPanel CopyButton pattern, the "Clear cache" button text briefly flips to `clearCacheOk` for 1.5s with a `--status-ok` accent ring, then reverts; `isOverwrite` derived from `saved.some((q) => q.name.toLowerCase() === trimmedName.toLowerCase())`; new `.saveBarWrap` holds the bar + a conditional `.overwriteHint` (warn-coloured inline text describing the overwrite). `MetricsExplorer.module.css`: new `.saveBarWrap` / `.overwriteHint` / `.saveBar input:disabled { opacity: 0.6; }` / `.btnSmallOk` (`--status-ok` border + foreground). 4 new i18n leaves: `metricsExplorer.saved.{updateAction, overwriteHint, saving, clearCacheOk}` (EN + ZH). 2 new i18n tests: `preserves the pass-19 overwrite-action / saving / clearCacheOk wording` (pins the 4 canonical en/zh values + a verb check on `overwriteHint` — en must contain `overwrite`, zh must contain `覆盖`) and `distinguishes saveAction (Save) from updateAction (Update) — they must not collapse` (regression guard against a refactor that collapses them into one string). The existing `metricsExplorer.saved.* sub-keys in both locales` loop grows from 10 to 14 keys. tsc / vitest 382 (380 → 382, +2 new) / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-19.md](qa-v0.2.4-pass-2026-08-03-19.md) |
| 20 | 2026-08-03 | ForwardsBar tooltip + i18n (post-rotation follow-up, round 6) | 1 high + 1 medium. **High #1** (ForwardsBar): the per-pill error tooltip at `ForwardsBar.tsx:79` was a multi-line HTML `title` attribute — `return f.error ? \`${base}\n${f.error}\` : base;` — but `title` attributes collapse `\n` to a space in every browser, so the resolved target and the failure string ended up glued together with no visual separator (e.g. `default/pod mypod:80 connection refused`). The mock provider doesn't fail forwards in the demo so the defect doesn't show up in the in-app visual loop, but the real TauriProvider can fail a forward (`port already in use` from the kernel bind) and the tooltip is the only place the user reads the error inline. **Medium #2** (i18n test pins): the `chrome.forwards.{label, copyAddress, stopForward, podTarget, serviceTarget}` block was never pinned by an i18n test — a future refactor could drop any of the 5 the way `chrome.palette.actions.*` / `topology.*` were dropped in earlier passes. Observed (not fixed): in-app Browser render queue stuck, 11th consecutive pass — treat as known-stable harness issue. Observed (not fixed, follow-ups still on the queue): pass-13 (Templates empty text fields `required` policy), pass-15 (MCP server health / latency), pass-16 (resource table column resize / reorder UX), pass-17 (cluster switcher mid-dot version truncation + WatchFooter pulsing dot), pass-14/17 (`title="Grafana"` brand string). **The v0.2.4 rotation + 6 rounds of post-rotation polish is now substantive** — every major form surface (HelmMarket add-repo, Templates, ActionList scale, ActionList port-forward, MetricsExplorer save bar, MetricsExplorer cache-bust) has been audited against the pass-12/13/18/19 fix pattern; the ForwardsBar tooltip / i18n gap is now closed; the chrome's residual i18n leaks (pass-15/16/17 sweep) are all addressed; and the `title=` `\n` class of bug is eliminated from the codebase (`grep` for `\n` in any `title=` attribute across `src/components/` returns no results). | `9af012c` — `ForwardsBar.tsx`: doc block gains a v2 note documenting why the join is ` — ` not `\n` (the em-dash is the same separator `TemplatePicker.tsx:159` uses for its per-row error join, so error copy reads consistently across the chrome); the `tooltip()` return becomes `return f.error ? \`${base} — ${f.error}\` : base;` (single-line separator that `title` attributes can render). 4 new i18n tests in `i18n.test.ts` (one `describe` block): `chrome.forwards.{label, copyAddress, stopForward}` pinned to canonical en/zh; `chrome.forwards.podTarget` pinned at `("default", "nginx-0", 80)` → `default/pod nginx-0:80` in both locales, with structural asserts (the noun "pod", the slash separator, the colon before the port); `chrome.forwards.serviceTarget` pinned at the full 5-arg signature → `default/service nginx:80 → pod nginx-0:8080`, with an explicit assert on the `→` arrow so a refactor that drops the published/resolved-port distinction trips the test; a "zh doesn't collapse to en" check on the 3 label / tooltip strings. tsc / vitest 386 (382 → 386, +4 new) / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-20.md](qa-v0.2.4-pass-2026-08-03-20.md) |
| 21 | 2026-08-03 | Sidebar chrome polish — cluster-switcher version overflow + WatchFooter state-aware dot (post-rotation follow-up, round 7) | 2 high + 2 medium. **High #1** (ClusterSwitcher): `statusText` at `ClusterSwitcher.tsx:84-87` was a raw text node inside a flex container with no `min-width: 0` / `overflow: hidden` / `text-overflow: ellipsis` — a long real-cluster version string like `v1.30.0-alpha.1+abcdef123456` pushed the `▼` chevron off the right edge of the switcher button. The mock provider's `v1.31` is short enough to fit, so the defect doesn't show in the in-app visual loop, but any real Kubernetes cluster version trips it. **High #2** (WatchFooter): the dot at `WatchFooter.tsx:18` was a single `<span className={styles.footerDot} />` with the `livePulse 2s ease-in-out infinite` animation running unconditionally — the chrome's two status indicators (top cluster switcher + bottom footer dot) were contradicting each other on disconnect (red on top, green pulsing on the bottom). **Medium #3** (ClusterSwitcher): the cluster name had `text-overflow: ellipsis` in CSS but no `title` attribute — a long truncated name had no way to read the full thing on hover. **Medium #4** (i18n test pins): `chrome.sidebar.watch(0)` not pinned — the watch count drops to 0 during disconnect (B11 lifecycle), and a refactor that broke the N=0 path would compile cleanly and the `translate()` helper would fall back to the English copy in zh. Observed (not fixed): in-app Browser render queue stuck, 12th consecutive pass — treat as known-stable harness issue. Observed (not fixed, follow-ups still on the queue): pass-13 (Templates empty text fields `required` policy), pass-15 (MCP panel card polish — observation rather than a live-health fix), pass-16 (resource table column resize / reorder UX), pass-14/17 (`title="Grafana"` brand string — by-design, the iframe's accessible name). **The v0.2.4 rotation + 7 rounds of post-rotation polish is now substantive** — every major form surface (HelmMarket add-repo, Templates, ActionList scale, ActionList port-forward, MetricsExplorer save bar, MetricsExplorer cache-bust) has been audited against the pass-12/13/18/19 fix pattern, the ForwardsBar tooltip / i18n gap is closed, the chrome's residual i18n leaks (pass-15/16/17 sweep) are all addressed, the cluster switcher version overflow is closed, and the WatchFooter dot now reflects the connection lifecycle (pulsing = live, static = not connected). | `19dc3e1` — `ClusterSwitcher.tsx`: statusText wrapped in `<span className={styles.statusText} title={statusText}>` so the flex child can shrink + ellipsis and the full text is on hover; clusterName div gains `title={name}` for the same reason. `Sidebar.module.css`: new `.statusText` class with `min-width: 0; flex: 1; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;` (same pattern `PropertiesTab.tsx:125` and `NavList.tsx:116, 204, 219` use for truncation + hover). `.footerDot` loses its hardcoded `background: var(--accent)` (now inline) and its unconditional `animation: livePulse …` (moved to a new `.footerDotPulsing` class); the halo + glow stays tied to accent because only the connected-state dot earns the "live" treatment. `WatchFooter.tsx`: subscribes to `connection.phase` alongside `watchCount`; new `dotColor` derivation picks accent / warn / err / text-faint based on phase, `pulse = phase === "connected"` gates the animation. New v2 doc block records the rationale. `i18n.test.ts`: new `chrome.sidebar.watch(0)` test pins the disconnect-time case in both locales. tsc / vitest 387 (386 → 387, +1 new) / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-21.md](qa-v0.2.4-pass-2026-08-03-21.md) |
| 22 | 2026-08-03 | Template picker form `required` policy (post-rotation follow-up, round 8) | 1 high + 1 low. **High #1** (`TemplatePicker.tsx:114-125`): every text/number input across the 3 templates (deployment / ingress / configmap) was missing the native `required` HTML5 attribute. The renderer at `templates.ts:104-109, 188-194, 240-246` silently substitutes the default via `v.name || "my-app"`-style `||` fallback — a user who clears the "Name" field and clicks Apply sees a successful `my-app` apply with no error, the exact failure mode pass-13 explicitly flagged as a follow-up. **Low #2** (schema): the `required` decision was buried in a JSX expression with no place in the `TemplateParam` schema to flip the policy. Fix follows the same pattern as pass-13's `min`/`max`: schema carries `required?: boolean`, form mirrors it mechanically, tests pin the policy. Observed (not fixed): in-app Browser render queue stuck, 13th consecutive pass — treat as known-stable harness issue. Observed (not fixed, follow-ups still on the queue): pass-15 (MCP panel card polish — observation rather than a live-health fix), pass-16 (resource table column resize / reorder UX — feature-sized, out of scope for these targeted passes), pass-14/17 (`title="Grafana"` brand string — by-design, the iframe's accessible name). **The v0.2.4 rotation + 8 rounds of post-rotation polish is now substantive** — every major form surface (HelmMarket add-repo, Templates, ActionList scale, ActionList port-forward, MetricsExplorer save bar, MetricsExplorer cache-bust) has been audited against the pass-12/13/18/19 fix pattern, the ForwardsBar tooltip / i18n gap is closed, the chrome's residual i18n leaks are all addressed, the cluster switcher version overflow is closed, the WatchFooter dot now reflects the connection lifecycle, **and the Templates form now has the full required+min+max+Enter-to-submit+namespace-in-result polish**. | `d9c72e4` — `TemplateParam` gains `required?: boolean` (doc block explains the policy: text/number default `true`, boolean default `false`); `TemplatePicker.tsx:114-125` text/number input gains `required={p.required ?? true}` with a 6-line JSX comment explaining the narrowing (the boolean branch is handled above, so by the else branch `p.kind` is `"text" | "number"` and the default is `true`); renderer's `||` fallback remains a safety net for programmatic callers (tests, future programmatic apply APIs). 2 new tests in `templates.test.ts`: `every text/number param is required (no opt-out yet)` (asserts `p.required ?? true === true` for every non-boolean param across every template, with `${t.id}.${p.key} (${p.kind})` error message so a future template that ships an unmarked optional field fails loudly) and `the form's default for \`required\` is \`true\` for text/number and \`false\` for boolean` (re-implements the `required ?? kind !== "boolean"` default locally so a refactor that flips the form's default trips the test). tsc / vitest 389 (387 → 389, +2 new) / cargo check all clean; pushed to `origin/main`. | [qa-v0.2.4-pass-2026-08-03-22.md](qa-v0.2.4-pass-2026-08-03-22.md) |
