--- a/gen/android/buildSrc/src/main/java/io/k7s/app/kotlin/BuildTask.kt
+++ b/gen/android/buildSrc/src/main/java/io/k7s/app/kotlin/BuildTask.kt
@@ -52,6 +52,9 @@
             executable(executable)
             args(args)
+            // Inject --cfg mobile so cfg_attr(mobile, mobile_entry_point) expands.
+            // The Tauri Gradle plugin overrides CARGO_TARGET_*_RUSTFLAGS.
+            environment("CARGO_ENCODED_RUSTFLAGS", "--cfg\u001fmobile")
             if (project.logger.isEnabled(LogLevel.DEBUG)) {
                 args("-vv")
             } else if (project.logger.isEnabled(LogLevel.INFO)) {
