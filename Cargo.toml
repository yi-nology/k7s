[workspace]
resolver = "2"
members = [
    "crates/k7s-core",
    "src-tauri",
]
exclude = [
    "crates/k7s-deps",
    # Standalone probe binary with its own lockfile — not part of the workspace.
    "crates/probe",
]

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
strip = true
